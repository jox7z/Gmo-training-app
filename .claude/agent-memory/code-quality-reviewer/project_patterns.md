---
name: project-patterns
description: Architecture patterns, optimistic update conventions, and recurring anti-patterns in the Gmo Training App
metadata:
  type: project
---

## Optimistic update pattern (TanStack Query)
- onMutate: cancel queries, snapshot prev, patch cache, return context
- onError: restore from context snapshot (reverts query cache only)
- onSettled: invalidate (no refetch on sensitive lists to avoid scroll reset)
- **Critical gap**: frozen local state in connections.tsx is NOT covered by onError rollbacks — it diverges from the global cache on mutation failure. See [[frozen-divergence-bug]].

## Frozen list pattern (connections.tsx)
- First-load data is frozen into local state to prevent rows disappearing on unfollow (Instagram style)
- handleFollowChange patches `isFollowing` on frozen rows optimistically
- handleRefresh clears frozen + refetches
- Bug: `frozen` is never reset when `type` (followers/following) or `targetUsername` changes while the component stays mounted (e.g. param change via router). [[frozen-stale-on-param-change]]

## ExercisePickerModal dropdown
- `groupOpen` state is never reset to false when the modal is hidden (visible=false) and re-shown — it can open with the dropdown already expanded
- No zIndex issues for inline dropdown since it sits above ScrollView in the tree

## FeedItem ActionButton
- `label` is now `''` (empty string) when no reaction is active — component conditionally omits the Text node, which is correct
- The reaction button's emoji fallback '👊' is always rendered, so the button never collapses to zero width — layout is stable

## Redesign: Button 3D chunky pattern (added 2026-06-09)
- Button has chunky 3D mode for md/lg sizes when variant has `edgeColor` (primary, accent, secondary, danger). Ghost never chunky. Use `flat` prop to disable.
- Chunky Pressable wraps: `{ paddingBottom: depth.edge }` + absolute `edge` View + `face` View that translateY on press.
- `style` prop is applied to the outer Pressable, NOT the face — callers using `style={{ flex: 1 }}` will flex the Pressable container but the face won't stretch unless it has `alignSelf: stretch` or `flex: 1` internally. This is a known hitbox/layout quirk.
- depth token: `{ edge: 4, edgeLg: 5, pressTravel: 4 }`.

## Redesign: Workout session components (added 2026-06-09)
- Phase machine: warmup → set → log → rest → (loop) → summary
- `getNextPosition()` is a pure replica of `advancePosition()` logic. Must stay in sync.
- `swapExercise(exIdx, newId)` returns new index (may shift if completed sets exist). `handleSwapSelect` uses the returned index correctly.
- `playSplash(phrase)`: extracted helper, closes over stable Animated.Value refs — safe.
- RestRing: strokeDashoffset uses `useNativeDriver: false` — correct, SVG props not supported by native driver.
- SetProgressPills: Animated.loop with `loop.stop()` cleanup — no leak.
- `usedExerciseIds` memoized off `active?.exercises` — stable reference pattern, correct.

## Instagram: OAuth removed, manual only (updated 2026-06-12)
- OAuth client flow (`linkInstagram`, `handleLink`, `handleUnlink`, deep-link `useEffect`, `useLocalSearchParams` for `ig_status`) fully deleted from `app/profile/edit.tsx`
- `src/lib/instagram.ts` deleted; no remaining imports anywhere
- Only remaining client surface: manual `instagram_username` input in `edit.tsx`, display in `profile.tsx` and `profile/[username].tsx` via `openInstagram` from `src/lib/linking.ts`
- `instagramVerified` field still lives in types/store/repos (read from DB) but no badge is shown; field is kept for data compatibility with existing rows
- Trigger `protect_instagram_verification` on `public.profiles` still active (migration 0039) — blocks client writes to `instagram_verified`, `instagram_user_id`, `instagram_linked_at`
- `toDb()` in `src/lib/repos/profile.ts` still correctly omits all three protected columns
- Edge function `instagram_oauth` still deployed but inert (no client entry point)
- `expo-web-browser` still in `package.json` — harmless leftover, used by edge function deploy notes only

## Redesign: Known issues found in 2026-06-09 review
- Button chunky: `fullWidth` uses `alignSelf: stretch` on the Pressable but face View has no matching width — face won't stretch to full width. Should add `width: '100%'` or `alignSelf: 'stretch'` to the face.
- RestPhase pills: `pillsCurrent` when `next` is null uses `currentSetIdx` (0-based, the just-completed set). But SetProgressPills treats `current` as the active-but-not-done index. On the last set, pills show the last set as "current/pulsing" instead of all filled — minor visual inaccuracy.
- `playSplash` is defined inside the component body as a plain function (not useCallback) — it is recreated every render. Called only from a useEffect and an event handler, not from render, so no re-render cascade; however the phase-change useEffect has `// eslint-disable-next-line react-hooks/exhaustive-deps` suppressing the `playSplash` dep — intentional to avoid re-running on every render, but creates a stale closure risk if `playSplash` ever reads render-cycle state.
- SwapExerciseModal: image thumbnail inline (not extracted to ExerciseThumb) — minor duplication.
- `interval` variable in PrCarousel autoplay useEffect dep array: recalculated every render based on screenWidth; stable in practice but causes a new setInterval registration on every resize event (landscape flip). Low risk on mobile.

## Achievements system (added 2026-06-12)
- Motor puro en `src/lib/achievements.ts`: catálogo ACHIEVEMENTS, evaluateTrack, evaluateAchievements, unlockedTierIds, groupByCategory, lookupTier. Todo derivado del historial local + streakWeeks — fully offline.
- Store persistido en `src/store/achievements.ts` (AsyncStorage 'gmo:achievements:v1'): unlocked Record<tierId, ISO date>, seeded flag. sync() es síncrono (no async), persiste via fire-and-forget.
- Known bug: Animated.sequence in AchievementUnlockModal never stopped on unmount/index-change — can fire callbacks after component unmounts. Fix: store .start() return value and call .stop() in useEffect cleanup.
- Known issue: weekIndex() uses UTC epoch arithmetic (floor(ms/86400000)), not local calendar. On UTC-N timezones, midnight train logs before UTC day-change assign to yesterday's week. Low impact (gym hours rarely cross UTC midnight for US/EU users), but not locale-correct.
- Known issue: achievements tab in profile.tsx calls evaluateAchievements() in useMemo, but earnedLevels/totalLevels are re-computed inline without memoization — two separate reduce passes on the same array. Minor; consolidate if profile screen grows.
- persist() in achievements store snapshots state at call time but is called immediately after set() — correct ordering confirmed.
- seeded flag: backfill logic in _layout.tsx reads seeded AFTER Promise.all hydration — correct; race-safe because all stores hydrate before the seeded check runs.

## Roadmap sprint 3 (added 2026-07-12, worktree roadmap-sprint1, branch feat/roadmap-sprint3)
- `src/components/Icon.tsx` rewritten as facade over `lucide-react-native@1.24.0`: `REGISTRY: Record<Exclude<IconName,'scale'|'instagram'>, RegistryEntry>` — TS exhaustiveness on an inline object literal catches both missing AND excess keys, so registry/type drift is compiler-enforced, not just convention. Verified: all 156 call sites are typed through `IconName`-typed props (no raw string bypass), so a bad name would be a typecheck error, not a runtime crash.
- Dead icon name `'robot'` (leftover from the removed AI coach, see CLAUDE.md `0040_drop_ai_coach.sql`) was NOT removed in the C2-3 cleanup even though sibling dead names `props`/`respect`/`clap` were — zero call sites reference it. Low-severity inconsistency, not a bug.
- `'barbell'` intentionally kept as an alias for the `Dumbbell` lucide glyph — genuinely used by `src/lib/achievements.ts` `strengthTrack(...)` calls (bench-press/squat/deadlift/overhead-press icons), confirmed via grep, not dead.
- `src/lib/plates.ts` `calcPlates(target, bar, unit)`: greedy per-side plate breakdown, EPS=0.001 float tolerance, `target <= bar + EPS` short-circuits to "just the bar" with remainder 0. Correct for target<bar, target==bar, and float noise (2.5/1.25 kg increments). `src/components/workout/PlateCalculatorSheet.tsx` consumes it entirely in **display units** (kg or lb, whatever `unit` prop is) — conversion to kg happens only in the caller (`app/workout/active.tsx` LogPhase's `onWeightChange={(v) => updateSet(..., { weightKg: fromDisplay(v, profile.unit) })}`), same as `BigStepperInput.onChange`. Both paths agree on units, no bug.
- `BigStepperInput` (`src/components/workout/BigStepperInput.tsx`) commits typed-but-unsubmitted text on `onBlur` (`parseFloat(text)` → `onChange`), so tapping the new "Discos" `PressableScale` button while the decimal-pad is focused correctly commits the in-progress edit before the plate sheet reads `set.weightKg` — no stale-value race.
- `AppBottomSheet` default `keyboardBlurBehavior="restore"` (shared across all sheets, not new in this sprint) means closing `PlateCalculatorSheet` while the weight `TextInput` was previously focused will re-summon the keyboard. Pre-existing shared-component behavior, not introduced by C2-8 — don't flag in isolation.
- `src/lib/optimizationScore.ts` `weekIndexOf()` (added for C2-9) is a byte-for-byte replica of `src/lib/achievements.ts` `weekIndex()` (local-midnight epoch day, `+3` Monday alignment) — confirmed identical, "current week" is consistent between the heatmap and the streak engine. `weeklySetsByMuscle`/`muscleStatusFromWeeklySets` reuse the exact same `MIN_WEEKLY_SETS`/`OPTIMAL_MAX_SETS`/`SECONDARY_WEIGHT` thresholds as `analyzeRoutineMuscles` (routine-level table) — verified line-for-line match.
- `WeeklyMuscleHeatmapCard` (`src/components/WeeklyMuscleHeatmapCard.tsx`) memoizes `weeklySetsByMuscle(history)` on `[history]` only, using `Date.now()` internally — if the app stays open/mounted across the Sunday→Monday week boundary with no new workout logged, the card won't re-derive "current week" until `history` changes next. Cosmetic/low-severity, same class of staleness as other `Date.now()`-in-useMemo patterns in the app.
- `MuscleMap` (`src/components/MuscleMap.tsx`) is **pre-existing** (already used by `app/(tabs)/routines.tsx`), built on `react-native-body-highlighter` — C2-9 only added the new `WeeklyMuscleHeatmapCard` wrapper around it, did not touch `MuscleMap.tsx` itself. `full_body`-muscle exercises are deliberately omitted from the heatmap (documented in-file) since the silhouette has no matching slug; this can make `isEmpty` show "Aún no entrenas esta semana" even after a full-body-only session — matches the documented scope decision, not a bug.
- `TabIcon.tsx`'s `focused` prop was already unused/ignored before this sprint (color is passed in by the tab navigator itself) — the lucide migration didn't change that behavior, confirmed via diff.

## Roadmap sprint 4 (added 2026-07-13, worktree roadmap-sprint1, branch feat/roadmap-sprint4)
- Lint baseline verified by hand: `git worktree add` at `d1a11ed` + `npm run lint` gives exactly 22 warnings (0 errors); HEAD of sprint4 gives exactly 21 (0 errors) — the only diff is `src/components/feed/reactions.ts` `'colors' is defined but never used` disappearing because `colors.accent.DEFAULT` replaced the `'#ff8000'` literal. No new warnings introduced anywhere else (confirmed line-by-line against baseline output, including the pre-existing `'ExercisePeriod' is defined but never used` in `app/(tabs)/progress.tsx` which already existed before this sprint, just shifted one line).
- `src/components/ExerciseDetailSheet.tsx` (C2-11) absorbs the deleted `ExerciseProgressModal.tsx` as its Historial tab — diffed byte-for-byte against `git show d1a11ed:src/components/ExerciseProgressModal.tsx`: stats calc, PR detection (`detectPRs`), session list, chart all identical logic, just re-skinned onto `SegmentedControl`. Confirmed no parity regression.
- Hub remount mechanism: `app/workout/active.tsx` renders `<ExerciseDetailSheet key={currentEx?.exerciseId} .../>` and `app/records.tsx` renders `{detailId && <ExerciseDetailSheet key={detailId} .../>}` (detailId retained across close so key is stable on dismiss, only changes when a *different* card/exercise opens). Verified safe: `AppBottomSheet`'s `BottomSheetModal` is always mounted and driven by `visible` via `.present()/.dismiss()` (not conditional rendering), so a `key` change while `visible=true` would hard-remount and skip the close animation — but in both callers the key only changes while the sheet is *closed* (workout UI has no path to advance/swap exercise while the full-screen 80% sheet is open; records taps are blocked by the same sheet covering the list). Don't add a code path that changes `exIdx`/`detailId` while the sheet is visible without revisiting this invariant.
- `src/components/ui/Text.tsx` gained fixed `lineHeight` on `display`(53)/`metric`(44)/`metricLg`(78)/`timer`(68) variants (previously undefined/natural). Two **pre-existing** call sites in `app/(tabs)/progress.tsx` (`AveragesCard`, lines ~235/241) combine `variant="metric"` with `adjustsFontSizeToFit numberOfLines={1}` — combining a fixed `lineHeight` with `adjustsFontSizeToFit` is a known RN/Android footgun (shrunk font can mis-center/clip inside a line box sized for the unshrunk font). Flagged for on-device verification, not confirmed broken (can't render RN here). Other `metric`/`metricLg`/`display` call sites (`Stat.tsx`, `StreakRing.tsx`, `FeedItem.tsx` PR line, onboarding "days") have no fixed-height container or autosizing, so the new lineHeight is safe there.
- `letterSpacing` token sweep (`8b7f7f2`+`f457cc1`) rounds some inline values to the nearest scale step: `3→widest(4)`, `0.5→snug(0.3)`, `-0.3→tight(-0.5)` — all deliberate/accepted, don't re-flag. One inline `letterSpacing: 1` survives untouched at `app/(tabs)/profile.tsx:180` (rank pill label) — missed by the sweep, cosmetic, low priority.
- `IconButton` badge (bell icon, `app/(tabs)/index.tsx`) changed source color from literal `colors.danger` (#EF4444) to the primitive's hardcoded `colors.primary.DEFAULT` (#FF3B3B) and position from inset (top:4/right:4 on a 16×16 dot) to edge-hugging (top:-2/right:-2). Both are red hues in this red-branded app so visually close, but it is a real token substitution (brand/CTA red now doubles as the "unread count" affordance) — `IconButton` has no `badgeColor` prop to override this.
- Hex sweep (`5ae1728` app/, `65dd5ad` src/) is thorough but not 100%: `src/components/feed/FeedItem.tsx` still has raw `'rgba(255,215,0,0.4)'` (lines ~145, ~223), `'rgba(255,215,0,0.5)'` (~390), and two Animated interpolate `outputRange`s with raw gold rgba (~269, ~353) — `colors.medal.goldBorder` (`rgba(255,215,0,0.45)`) already exists for exactly this purpose but wasn't applied here. Not in the commit's documented exception list. Same file/gap already noted in the sprint-1 memory entry below; still not closed out.
- New primitives `SegmentedControl`/`IconButton`/`Chip` (`d5265de`): every caller across the sprint passes a **module-level** `options` array (never an inline literal recreated per render) — no perf issue found. `Chip` has no `numeric` (tabular-nums) passthrough, so `PlateCalculatorSheet`'s bar-weight chips (e.g. "Barra 20 kg") lost tabular-nums vs. the original inline `Pressable`+`Text numeric` — cosmetic only.
- `FORMULA_OPTIONS` (`ONE_RM_FORMULAS.map(...)`) is duplicated verbatim as a module const in both `app/records.tsx` and `src/components/ExerciseDetailSheet.tsx` — minor DRY gap, harmless.

## Roadmap sprint 1 (added 2026-07-11, worktree roadmap-sprint1)
- `src/theme/tokens.ts` gained `colors.medal.{gold,silver,bronze,goldSoft,goldBorder}` + `podiumColor(position)`. Callers in discover.tsx/progress.tsx/events/[id].tsx/WorkoutResultsModal/FeedItem migrated cleanly, semantics preserved exactly (verified 1:1 diff). **Incomplete migration**: `src/components/feed/FeedItem.tsx` PrBadgeRow/PrCarousel (lines ~139-227, ~310-311) still has raw `'#FFD700'` / `'rgba(255,215,0,...)'` literals untouched — pre-existing, out of this diff's scope, but worth finishing next time gold tokens are touched in that file.
- `src/lib/workoutCompare.ts`: `historicMaxWeight(history, exerciseId, excludeWorkoutId)` extracted from `detectPRs`'s inline calc — confirmed byte-for-byte same filter/flatMap/reduce logic, no behavior change. `previousExerciseSets(history, exerciseId, excludeWorkoutId)` assumes `history` sorted descending by `startedAt` (true: `finishWorkout` in `src/store/workouts.ts` does `[finished, ...history]`, `mergeHistory` re-sorts descending after merging remote rows) — safe to rely on this invariant elsewhere.
- **Zustand full-object-selector re-render trap**: selecting `s.active` (the in-progress Workout) whole in a component gives a new reference on every `updateSet` call (every stepper tap in `app/workout/active.tsx` LogPhase). Any `useMemo` keyed on that whole object, or any *other mounted* component subscribed to `s.active`, recomputes/re-renders on every keystroke of the active session — not just on exercise/set-index change. Fixed 2 instances in this sprint: `prevSets` useMemo in active.tsx (now keyed on `exerciseId`+`activeId` primitives, not `active`), and `StartWorkoutFab` (renders inside `app/(tabs)/index.tsx`, which stays mounted permanently because the tabs layout is a `PagerView` rendering all 4 tabs at once, not lazily — so it was re-rendering on every weight/rep edit during a live workout). Pattern to watch for: prefer primitive selectors (`s.active?.routineDayId`, `s.active !== null`) over the whole `s.active` object in any component that isn't the active-workout screen itself.
- `src/lib/tabsNav.ts`: singleton `registerTabSetter`/`goToTab` bridge so components outside the `(tabs)` PagerView layout (e.g. `StartWorkoutFab`) can switch tabs. Registered in `app/(tabs)/_layout.tsx` via `useEffect` returning the unregister fn — correctly cleaned up, no leak.
- `src/lib/oneRepMax.ts` `estimate1RM`: reps clamped to [1,30] via `Math.min(30, Math.max(1, Math.round(reps)))` before formula — Brzycki's `36/(37-r)` therefore never divides by zero (worst case r=30 → denom=7). `weightKg <= 0` short-circuits to 0 (bodyweight). `computeExerciseRecords` is a single O(n) pass over history/exercises/sets, filters `isCompleted && !isWarmup && weightKg > 0` — consistent with the same filter used elsewhere (`exerciseTopWeight`, achievements strength track).
- `app/records.tsx` whitelisted correctly in `app/_layout.tsx` `inAllowedAuthedRoute` (`first === 'records'`) and has a matching `<Stack.Screen name="records">`.

## Roadmap sprint 5 (added 2026-07-14, worktree roadmap-sprint1, branch feat/roadmap-sprint5)
- Jest runner added (`jest-expo` preset, `moduleNameMapper` for `@/*`, `testMatch` scoped to `**/__tests__/**/*.test.ts?(x)`). 7 suites / 54 tests, all genuine behavior assertions (not tautological). `src/lib/__tests__/fixtures.ts` builders (`makeWorkout`/`makeExercise`/`makeSet`) use `import type` for `Workout`/`WorkoutExercise`/`SetEntry` from `@/store/workouts` specifically to avoid dragging in AsyncStorage at test time — follow this pattern for any future store-type test fixture. Date-sensitive suites (`optimizationScore.test.ts`, `achievements.test.ts`) inject `now`/use `jest.useFakeTimers().setSystemTime(...)` with local `new Date(year, month, day, ...)` components (never UTC ISO strings for "today") — immune to timezone/day-of-run flakiness, good pattern to replicate.
- **`networkMode: 'offlineFirst'` (`app/_layout.tsx:74-84`) does NOT make queries fail fast to `isError` while offline, contradicting its own code comment.** Verified against `node_modules/@tanstack/query-core/build/modern/retryer.js`: `canFetch()` (the gate for the *first* attempt) ignores connectivity for `offlineFirst`/`always`, but the *retry* path (`canContinue()`, used after every failure) still requires `onlineManager.isOnline()` unless mode is `'always'` — `offlineFirst` is NOT exempted there. Combined with the pre-existing global `retry: 1` (queryClient default, `app/_layout.tsx:71`), a query with **no cached data** that fails once while offline gets scheduled for one retry, which then *pauses* (`fetchStatus: 'paused'`) instead of rejecting. While paused: `isFetching=false` → `isLoading=false` (queryObserver.js:310), but `status` stays `'pending'` → `isError` never becomes `true`. Every screen using the repeated `isInitialLoading / hasError / isEmpty` pattern (`app/notifications.tsx:158-160`, `app/(tabs)/index.tsx:269-271`, plus the `.isError` checks in `discover.tsx`/`progress.tsx`) falls through to the EMPTY branch, not the new ErrorState, in this exact scenario — the reverse of the sprint's stated goal. **Mutations are unaffected**: `mutation.js:83` hardcodes `retry: this.options.retry ?? 0` independent of the queries' global default, so mutations (optimistic updates in `src/lib/queries/feed.ts`) fail fast and roll back correctly, no silent pause. `isConnected: null` at NetInfo boot also isn't a real risk — `offlineFirst`'s first-attempt gate ignores `onlineManager` entirely, and NetInfo resolves the real state well before any retry-delay (~1s) elapses. If this comes up again: fix is either `retry: 0` for queries under `offlineFirst`, or explicitly branching on `isPaused` in the shared loading/empty/error derivation.
- **`app/profile/[username].tsx:100`** (`if (!isSelf && searchQuery.isError)`) is NOT gated on `!matched`/`!targetUserId`, unlike its sibling detail screens added in the *same* sprint: `app/events/[id].tsx:164` (`eventQuery.isError && !event`) and `app/communities/[id].tsx:173` (`communityQuery.isError && !community`), which correctly prioritize cached data. Confirmed via `query-core/build/modern/query.js:375-389`: the `"error"` reducer case never clears `data`, so a failed background refetch (e.g. pull-to-refresh offline) on an already-rendered profile still has `isError=true` with `matched`/`data` intact — this file's early-return replaces the whole screen with a full ErrorState anyway, hiding valid content. The other detail screens don't have this bug; treat `!matched`-style gating as the required pattern for any future full-screen `isError` early-return in this codebase.
- expo-image migration (FeedItem's 3 photo call sites + Avatar) preserves `aspectRatio: 4/5`, uses `recyclingKey={post.id}` correctly (needed since FeedItem recycles inside FlashList), `cachePolicy="memory-disk"`, and leaves Avatar's no-`uri` initials fallback branch untouched.

## Roadmap sprint 6 (added 2026-07-23, worktree roadmap-sprint1, branch feat/roadmap-sprint6)
- `src/lib/queryState.ts` — new pure fn `useQueryState({isLoading,isError,isEmpty}) => 'loading'|'error'|'empty'|'ok'` (named with `use` prefix but contains zero hook calls internally, so it's exempt from rules-of-hooks; safe even though 2 call sites are inside nested component functions in the same file, e.g. `TimelineCard`/`LeaderboardSection` in `app/(tabs)/progress.tsx`). Verified all ~10 call sites (`progress.tsx` ×2, `profile.tsx`, `profile/[username].tsx`, `profile/connections.tsx`, `communities/[id].tsx` ×3, `events/[id].tsx`, `notifications.tsx`) pass `isEmpty` correctly flattened for infinite queries (`.pages.flatMap(...)`) and plain length checks for regular queries — no call site passes a mis-flattened `isEmpty`. Contract (`isError && isEmpty` → 'error', else if `isEmpty` → 'empty', preserving cached content on background-refetch errors) is respected everywhere.
- `app/profile/[username].tsx:109` `if (!isSelf && searchQuery.isError && !matched)` — confirmed FIXED in this sprint (was previously ungated on `!matched`, flagged in sprint-5 memory entry above). Now consistent with `events/[id].tsx`/`communities/[id].tsx` sibling pattern.
- `app/profile/connections.tsx:124` `!isSelf && searchQuery.isError && !resolvedId` — correctly gated, no gap; `resolvedId` stays truthy from stale `searchQuery.data` even if a later refetch errors, so cached-content priority holds.
- `app/notifications.tsx`'s mark-all-read side effect moved from `useMemo` (side effect in render-phase hook, bug) to `useEffect` with dep array `[notifications.length === 0]` (boolean, fires once per empty→nonempty transition, deliberate/unchanged) + new `!markRead.isPending` guard both in the effect and in `handleMarkAll` — prevents double-fire from concurrent invocations (e.g. React 19 dev double-invoke). Correct fix, intent preserved.
- Chip consolidation (`events/new.tsx`, `events/edit/[id].tsx`): local `Chip` (active state used filled `colors.primary.muted` background) replaced by shared `src/components/ui/Chip.tsx` `variant="outline"` (selected state has NO background fill, just border+text color per its outline branch) — a real but minor visual regression (loses the soft-fill highlight on selected day/time chip), not caught by typecheck/lint. Low severity, cosmetic only.
- Dead-code sweep verified clean: `src/lib/routineGenerator.ts` and `src/components/ActivityCard.tsx` deletions have zero remaining references anywhere (grepped both `app/` and `src/`); `zod` fully removed from `package.json` dependencies; `RoutineOption` type move to `src/data/routineTemplates.ts` doesn't break `routine/templates.tsx` (its only real consumer).
- Docs update this sprint fixed `docs/roadmap.md`/`docs/roadmap-ui.md` accurately but did NOT touch the **pre-existing, still-stale** `README.md`/`docs/memory/overview.md` references to `src/lib/coach.ts` and the `coach` edge function — both were fully removed in migration `0040_drop_ai_coach.sql` per CLAUDE.md, yet README.md lines ~3/33/61/75/87/114 and `docs/memory/overview.md` lines ~7/14/24/34 still describe a live AI coach. Out of scope for this sprint's diff (CLAUDE.md already flags README as "partly stale"), but worth closing out next time docs are touched.

## Roadmap sprint 7 (added 2026-07-24, worktree roadmap-sprint1, branch feat/roadmap-sprint7, uncommitted)
Two parallel sub-features reviewed together: 2a custom exercises (`src/lib/repos/exercises.ts`, `src/lib/queries/exercises.ts`, `src/components/CreateExerciseSheet.tsx`, `src/data/exercises.ts` module cache, migration `0048_exercise_custom_policies.sql`) and 2b public routines (`src/lib/repos/routines.ts` `listPublicRoutines`/`getPublicRoutineDetail`, `src/lib/queries/routines.ts`, `app/routine/explore.tsx`, `app/routine/public/[id].tsx`). Both touch `app/routine/[id].tsx` in disjoint regions — no overlap/collision found (verified full-file read, not just diff).
- **Real bug found**: `src/data/exercises.ts` `exerciseById()` module cache (`customExercises`, populated by `CustomExerciseCacheSync` in `app/_layout.tsx` via `useEffect(() => setCustomExerciseCache(data ?? []), [data])`) is scoped to `listCustomExercises(userId)` = **only the current viewer's own custom exercises** (`.eq('created_by', userId)` in the repo). `app/routine/public/[id].tsx:126` calls `exerciseById(ex.exerciseId)` to render exercise names in someone else's public routine — if that routine uses a custom exercise owned by ITS author (not the viewer), the cache never contains it (RLS `"exercises read all" using (true)` would actually allow fetching it, the gap is purely client-side scoping), so it silently falls back to the raw UUID string as the displayed name. Not caught by typecheck/lint/tests since it's a data-shape-valid runtime gap. Confined to this one screen (the explore list screen doesn't render exercise names, only routine summaries).
- **Real bug found**: same module-cache pattern has a render-order race on *creation*, not just cross-user reads. `CreateExerciseSheet`'s `onSuccess` synchronously calls `onCreated(ex)` → `addExercise(ex.id)` → `setRoutine(...)` in `app/routine/[id].tsx`, which re-renders the day's exercise list calling `exerciseById(e.exerciseId)` for the brand-new id — in the SAME commit as the query-cache update that will eventually flow to `CustomExerciseCacheSync`. Since all React renders in a commit happen before any `useEffect`s fire, `RoutineEditor`'s render in that commit still sees the pre-update module cache and shows the raw `exerciseId` (UUID) as the exercise name instead of `ex.name`, until some UNRELATED future re-render of that screen picks up the (by-then updated) module cache. Cosmetic but real, and non-obvious — matches the general class of "module cache as a non-React escape hatch" risk to watch for anywhere `exerciseById`/`setCustomExerciseCache` is touched again.
- `updateCustomExercise` (repo) and `useDeleteCustomExercise` (query hook) are both fully implemented (including the `23503` FK-violation friendly-error translation, correctly modeled the same way as `src/lib/repos/workouts.ts`'s `23505` duplicate-key handling) but have **zero UI call sites** anywhere in `app/` or `src/` — no screen exposes edit or delete for a custom exercise yet. Mechanically correct code, just unwired/dead from a product standpoint this sprint. Not flagged as a bug, but worth surfacing so it isn't mistaken for "shipped."
- RLS verified clean: `0048_exercise_custom_policies.sql` UPDATE/DELETE policies both correctly scoped `using (auth.uid() = created_by and is_custom = true)` (DELETE) / `+ with check` same clause (UPDATE) — global catalog rows (`is_custom = false`) are never editable by any client. Pre-existing `routines` RLS (`0001_init.sql` `"routines read own or public"` = `auth.uid() = user_id or is_public = true`, `"routines write own"` = `auth.uid() = user_id` on all of insert/update/delete) already fully covers the new public-routine read path — no new routines migration was needed, confirmed correct call by the implementing agent.
- `getPublicRoutineDetail` fetches by id first (`.maybeSingle()`, no `is_public` in the WHERE clause) then checks `data.is_public !== true` in JS before returning, returning `null` (not throwing) either for nonexistent rows or for the owner's own private routines read through this path — correct "not found, not error" contract, and safe even without the JS check since RLS itself would already block a stranger's private routine at the DB level (defense in depth, not the only safety net).
- `listPublicRoutines`'s `profiles!inner(username, display_name, avatar_url)` embed only selects public-safe fields — no email/other PII leakage.
- `ExercisePickerSheet` catalog merge (`[...EXERCISES, ...(customExercises ?? [])]`) happens before ALL filters (`exclude`/`only`/`group`/search/`highlightMuscle`) — contract preserved, verified line-by-line. "Tuyo" badge correctly takes priority over "Equivalente" (`!item.isCustom && highlightMuscle...`) so they never stack, same visual container/style as the pre-existing badge.
- Both new query-key factory files (`exercisesKeys`, `routinesKeys` in `src/lib/queries/{exercises,routines}.ts`) follow the same object-of-functions pattern as the rest of the project (e.g. `feedKeys`) — no divergence between the two parallel agents' styles.

## Sprint 8: supersets MVP (reviewed 2026-07-24, worktree roadmap-sprint1, branch feat/roadmap-sprint7, uncommitted)
Superset pairs-of-2, auto warm-up sets, and custom-exercise edit/delete UI landed together. Two real bugs found in the superset MVP, both violating the code's own documented "exactly 2 contiguous members" invariant — neither caught by typecheck/lint/tests (77/77 green) since they're pure state-shape bugs, not type errors:
- **`src/store/workouts.ts` `swapExercise`**: when swapping a superset member AFTER completing ≥1 of its sets, the function's pre-existing split-into-two-array-entries behavior (keep completed remnant at `exIdx`, splice replacement at `exIdx+1`) combined with the new `supersetGroupId: cur.supersetGroupId` line (added on BOTH the remnant and the replacement) fuses 3 exercises into one contiguous group (`[A_completed(g), A_new(g), B(g)]`). `buildStepSequence` treats any contiguous same-groupId run as one group, so this becomes an unintended 3-way round-robin. Fix direction: the completed remnant should NOT keep `supersetGroupId` (it's done, no longer "in" the live pairing) — only the live replacement should inherit it.
- **`app/routine/[id].tsx` `groupSelected`**: calls `dissolveOrphanGroups` on `removeExercise` but NOT after regrouping — nothing in `toggleGroupSelect`/group-mode UI blocks selecting an exercise that's already in a different group. Re-pairing one half of an existing pair with a third exercise leaves the abandoned original partner with a stale non-empty `supersetGroupId` (silently violates the size-2 invariant; its "unlink" icon still renders even though it has no partner anymore). `src/lib/supersets.ts` handles size-1 "groups" benignly at the algorithm level (no crash), but it's dead/confusing state. CLAUDE.md's superset section documents "editar membresía sin deshacer" as deferred/unbuilt — but the UI doesn't actually prevent the action, it just corrupts silently instead of blocking or auto-cleaning.
- **Confirmed via test-file inspection** (`src/lib/__tests__/supersets.test.ts`) that `buildStepSequence` is purely index-based and ignores `isWarmup` — combined with `canAddWarmup` (`app/workout/active.tsx:436-442`) having no `!currentEx.supersetGroupId` guard, adding warm-up sets to a superset member interleaves those warmups against the partner's real work sets round-by-round (partner finishes all real sets while the other warms up, then trains solo afterward). Reachable via UI, not just theoretical — this was the exact interaction the task prompt asked to verify.
- What WAS done correctly: `steps` `useMemo` keyed by shape string (`supersetGroupId:sets.length` per exercise, not the whole `active` object) — follows the established anti-pattern fix from sprint 1 (see `prevSets`/`StartWorkoutFab` note above). Weight/reps preload correctly gated `next.exIdx === exIdx` and excludes `isWarmup` on both sides. Repo mapping (`superset_group_id`↔`supersetGroupId`) verified complete on both insert/upsert and select paths in `src/lib/repos/routines.ts` + `workouts.ts`, no partial-mapping gap. Migration `0049_supersets.sql` correct and confirmed applied to remote (nullable column, partial indexes, no RLS change needed — parent-join policies already cover it).
- Custom-exercise edit/delete (`useUpdateCustomExercise`, long-press action sheet in `ExercisePickerSheet`, centered confirm-delete `Modal`) is clean: module-cache writes (`updateCustomExerciseInCache`/`removeCustomExerciseFromCache`) run synchronously in `onSuccess` BEFORE `qc.setQueryData`, avoiding the stale-name race previously documented for the create path (sprint 7 entry above) — this is an improvement worth replicating if the create path is ever touched again. `editTarget`/`deleteTarget` are React state (stable references), so `CreateExerciseSheet`'s `useEffect([visible, exercise])` preload doesn't re-fire spuriously on unrelated re-renders — verified, not a bug.

### Sprint 7 Línea 4 (reviewed 2026-07-24, same branch/worktree, disjoint from 2a/2b above)
- `app/onboarding.tsx`: 7-step wizard migrated from `useState<Step>` + conditional render inside one `ScrollView` to `react-native-pager-view` (`scrollEnabled={false}`, nav only via `goTo(idx)` = `setStep` + `pagerRef.current?.setPage(idx)`). Diffed byte-for-byte: every `renderStep(s)` switch case body is identical to the old `{step === 'x' && (...)}` block, just re-wrapped in `return(...)`. Case bodies correctly reference component-level form state (`name`/`level`/`goal`/`days`/`options`), not a per-step variable — that's correct since form state is shared across steps, not a "wrong step's data" bug. `goTo()` clamps via `Math.max(0, Math.min(STEPS.length-1, nextIdx))`. `handlePageSelected` re-`setStep`s the same string literal `goTo()` already set → no loop (referential equality bail-out). Follows the exact same "all pages mounted eagerly, ref+setPage imperative" pattern already established in `app/(tabs)/_layout.tsx` (see sprint-1 entry above on PagerView-always-mounted re-render trap — same caveat could apply here if onboarding ever adds a `useMemo`/effect keyed off a whole-object selector, currently doesn't).
- `src/components/illustrations/` (`EmptyFeedIllustration`, `EmptyRoutineIllustration`, `EmptyProgressIllustration`, `EmptyRecordsIllustration`, `index.ts`): hand-built `react-native-svg` vectors (no new dependency, `react-native-svg@15.12.1` already present), all colors sourced from `tokens.ts` (`colors.primary.dark`, `colors.medal.goldSoft/bronze`, `colors.info.soft`, `colors.borderStrong` — verified all exist), default `size=132`. Wired into the shared `illustration` slot on `EmptyState.tsx` in the 4 *primary* empty states (`app/(tabs)/index.tsx` feed, `progress.tsx` no-history, `routines.tsx` no-active-routine, `app/records.tsx` no-PRs) — correctly NOT applied to `progress.tsx`'s secondary leaderboard-empty (`EmptyState icon="trophy" title="Sin datos para este rango"` around line 961).
- `EmptyState.tsx` illustration branch changed from `illustration ?? (...)` to `illustration != null ? <View style={{marginBottom: spacing.md}}>{illustration}</View> : (...)` — adds the missing marginBottom the circle already had. Fallback-to-circle still correct when `illustration` is `undefined` (the only real-world case; all 4 call sites always pass a JSX element).
- **Minor stale doc found**: `EmptyState.tsx` prop comment at the `illustration?: React.ReactNode;` declaration still says "Slot futuro para ilustraciones (C1); sin uso aún" — no longer true, now has 4 real consumers. Not fixed in this diff, flagged for next touch of this file.
